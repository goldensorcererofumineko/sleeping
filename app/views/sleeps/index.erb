<main class="main">
  <div class="inner">
    <h2>睡眠記録一覧</h2>
      <% if user_signed_in? %>
      <div class="greeting">
        <%= "こんにちは、#{current_user.nickname} さん" %>
      </div> 
      <% end %>
    <div class="card__wrapper">
       <%= render partial: "sleep",collection:@sleeps %>
    </div>
  </div>
</main>